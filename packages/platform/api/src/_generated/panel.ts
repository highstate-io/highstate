// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v6.33.5
// source: panel.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Struct } from "./google/protobuf/struct";

export const protobufPackage = "io.highstate.panel.v1";

export interface Panel {
  /** The name of the panel unique within the unit instance and worker. */
  name: string;
  /** The display metadata of the panel (title, description, icon, iconColor). */
  meta?: { [key: string]: any } | undefined;
}

export interface SetUnitPanelsRequest {
  /** The ID of the worker version serving the panels. */
  workerVersionId: string;
  /** The ID of the concrete worker instance serving the panels. */
  workerInstanceId: string;
  /** The ID of the unit state the panels are attached to. */
  stateId: string;
  /** The complete set of panels currently served for the unit. */
  panels: Panel[];
}

export interface SetUnitPanelsResponse {
  /** Stable panel IDs in the same order as the request panels. */
  panelIds: string[];
}

function createBasePanel(): Panel {
  return { name: "", meta: undefined };
}

export const Panel: MessageFns<Panel> = {
  encode(message: Panel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.meta !== undefined) {
      Struct.encode(Struct.wrap(message.meta), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Panel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePanel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.meta = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Panel {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      meta: isObject(object.meta) ? object.meta : undefined,
    };
  },

  toJSON(message: Panel): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.meta !== undefined) {
      obj.meta = message.meta;
    }
    return obj;
  },

  create(base?: DeepPartial<Panel>): Panel {
    return Panel.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Panel>): Panel {
    const message = createBasePanel();
    message.name = object.name ?? "";
    message.meta = object.meta ?? undefined;
    return message;
  },
};

function createBaseSetUnitPanelsRequest(): SetUnitPanelsRequest {
  return { workerVersionId: "", workerInstanceId: "", stateId: "", panels: [] };
}

export const SetUnitPanelsRequest: MessageFns<SetUnitPanelsRequest> = {
  encode(message: SetUnitPanelsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workerVersionId !== "") {
      writer.uint32(10).string(message.workerVersionId);
    }
    if (message.workerInstanceId !== "") {
      writer.uint32(18).string(message.workerInstanceId);
    }
    if (message.stateId !== "") {
      writer.uint32(26).string(message.stateId);
    }
    for (const v of message.panels) {
      Panel.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetUnitPanelsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetUnitPanelsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workerVersionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workerInstanceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.stateId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.panels.push(Panel.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetUnitPanelsRequest {
    return {
      workerVersionId: isSet(object.workerVersionId)
        ? globalThis.String(object.workerVersionId)
        : isSet(object.worker_version_id)
        ? globalThis.String(object.worker_version_id)
        : "",
      workerInstanceId: isSet(object.workerInstanceId)
        ? globalThis.String(object.workerInstanceId)
        : isSet(object.worker_instance_id)
        ? globalThis.String(object.worker_instance_id)
        : "",
      stateId: isSet(object.stateId)
        ? globalThis.String(object.stateId)
        : isSet(object.state_id)
        ? globalThis.String(object.state_id)
        : "",
      panels: globalThis.Array.isArray(object?.panels) ? object.panels.map((e: any) => Panel.fromJSON(e)) : [],
    };
  },

  toJSON(message: SetUnitPanelsRequest): unknown {
    const obj: any = {};
    if (message.workerVersionId !== "") {
      obj.workerVersionId = message.workerVersionId;
    }
    if (message.workerInstanceId !== "") {
      obj.workerInstanceId = message.workerInstanceId;
    }
    if (message.stateId !== "") {
      obj.stateId = message.stateId;
    }
    if (message.panels?.length) {
      obj.panels = message.panels.map((e) => Panel.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<SetUnitPanelsRequest>): SetUnitPanelsRequest {
    return SetUnitPanelsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetUnitPanelsRequest>): SetUnitPanelsRequest {
    const message = createBaseSetUnitPanelsRequest();
    message.workerVersionId = object.workerVersionId ?? "";
    message.workerInstanceId = object.workerInstanceId ?? "";
    message.stateId = object.stateId ?? "";
    message.panels = object.panels?.map((e) => Panel.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSetUnitPanelsResponse(): SetUnitPanelsResponse {
  return { panelIds: [] };
}

export const SetUnitPanelsResponse: MessageFns<SetUnitPanelsResponse> = {
  encode(message: SetUnitPanelsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.panelIds) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetUnitPanelsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetUnitPanelsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.panelIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetUnitPanelsResponse {
    return {
      panelIds: globalThis.Array.isArray(object?.panelIds)
        ? object.panelIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.panel_ids)
        ? object.panel_ids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: SetUnitPanelsResponse): unknown {
    const obj: any = {};
    if (message.panelIds?.length) {
      obj.panelIds = message.panelIds;
    }
    return obj;
  },

  create(base?: DeepPartial<SetUnitPanelsResponse>): SetUnitPanelsResponse {
    return SetUnitPanelsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetUnitPanelsResponse>): SetUnitPanelsResponse {
    const message = createBaseSetUnitPanelsResponse();
    message.panelIds = object.panelIds?.map((e) => e) || [];
    return message;
  },
};

export type PanelServiceDefinition = typeof PanelServiceDefinition;
export const PanelServiceDefinition = {
  name: "PanelService",
  fullName: "io.highstate.panel.v1.PanelService",
  methods: {
    /** Replaces the web panels served by a worker for a unit instance. */
    setUnitPanels: {
      name: "SetUnitPanels",
      requestType: SetUnitPanelsRequest as typeof SetUnitPanelsRequest,
      requestStream: false,
      responseType: SetUnitPanelsResponse as typeof SetUnitPanelsResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface PanelServiceImplementation<CallContextExt = {}> {
  /** Replaces the web panels served by a worker for a unit instance. */
  setUnitPanels(
    request: SetUnitPanelsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<SetUnitPanelsResponse>>;
}

export interface PanelServiceClient<CallOptionsExt = {}> {
  /** Replaces the web panels served by a worker for a unit instance. */
  setUnitPanels(
    request: DeepPartial<SetUnitPanelsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<SetUnitPanelsResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
